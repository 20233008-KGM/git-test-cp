# [오늘 업무 계획] — 260520-77

> **계획 생성 시각:** 2026-05-20 14:58:50  
> **상태:** `done`

---

## 오늘 목표 (한 줄)

검증 리포트 아카이브 정리에 보관 일수(`keep-days`) 기준을 추가해 장기 누적 파일 관리 정책을 강화한다.

---

## 수행 예정 작업

| 순서 | TODO | 내용 |
|------|------|------|
| 1 | 스크립트 확장 | `collect-verification-report.mjs`에 `--keep-days` 옵션 추가 |
| 2 | npm 커맨드 추가 | 개수+일수 정책을 함께 적용하는 trim 커맨드 추가 |
| 3 | 검증/문서 동기화 | 실행 결과 확인 후 `02`·`05`·`17`·`25`·인덱스 반영 |

---

## 승인

- [x] 「더 진행해, 인간 없이 할 수 있는 일이 남아있으면 계획제출하고 바로시작해」 · 2026-05-20 14:58

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 스크립트 확장 | `--keep-days` + `.env` 정책(`VERIFY_BUNDLE_KEEP_LATEST`, `VERIFY_BUNDLE_KEEP_DAYS`) 로딩 + `retentionPolicy.source` 추가 |
| npm 커맨드 추가 | `verify:bundle:save:archive:policy` + `verify:bundle:save:archive:env` 추가 |
| 검증/문서 동기화 | env 기반 정책 실행(keepLatest=3, keepDays=7) 및 문서 반영 완료 |
