# [오늘 업무 계획] — 260520-72

> **계획 생성 시각:** 2026-05-20 14:32:15  
> **상태:** `done`

---

## 오늘 목표 (한 줄)

human_action 검증 JSON과 E2E 스모크 dry-run JSON을 한 번에 수집하는 통합 검증 리포트 커맨드를 추가한다.

---

## 수행 예정 작업

| 순서 | TODO | 내용 |
|------|------|------|
| 1 | 통합 스크립트 추가 | `collect-verification-report.mjs`에서 `human:verify:json` + smoke dry-run JSON 결합 |
| 2 | npm 커맨드 연결 | 단일 실행 커맨드(`verify:bundle:json`)를 `package.json`에 추가 |
| 3 | 검증/문서 동기화 | 커맨드 실행 확인 후 `02`·`05`·`17`·`25`·인덱스 반영 |

---

## 승인

- [x] 「더 진행해, 인간 없이 할 수 있는 일이 남아있으면 계획제출하고 바로시작해」 · 2026-05-20 14:32

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 통합 스크립트 추가 | `collect-verification-report.mjs`로 `human:verify:json` + smoke dry-run JSON 결합 |
| npm 커맨드 연결 | `verify:bundle:json` 스크립트 추가 |
| 검증/문서 동기화 | `verify:bundle:json` 실행 통과 및 문서 반영 완료 |
