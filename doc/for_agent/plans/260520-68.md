# [오늘 업무 계획] — 260520-68

> **계획 생성 시각:** 2026-05-20 14:16:42  
> **상태:** `done`

---

## 오늘 목표 (한 줄)

E2E 스모크 테스트를 자격증명 유무에 따라 자동 분기 실행되도록 개선해 skip 소음을 줄이고 실행 신뢰도를 높인다.

---

## 수행 예정 작업

| 순서 | TODO | 내용 |
|------|------|------|
| 1 | 스모크 실행기 추가 | `scripts/run-e2e-smoke.mjs`를 추가해 `.env` 기반으로 smoke grep 범위를 자동 분기 |
| 2 | npm 스크립트 연결 | `test:e2e:smoke`를 실행기로 교체하고 `test:e2e:smoke:full`을 보존 |
| 3 | 검증 + 문서 동기화 | smoke 실행 확인 후 `02`·`05`·`17`·`25`·`current_session_plan` 반영 |

---

## 승인

- [x] 「이제 작업 진행해,, 계획 제출하고 바로시작해」 · 2026-05-20 14:16

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 스모크 실행기 추가 | `scripts/run-e2e-smoke.mjs` 추가 (`.env` 자격증명 유무 자동 분기) |
| npm 스크립트 연결 | `test:e2e:smoke` 자동 분기 실행 + `smoke:public`/`smoke:full` 분리 |
| 검증 + 문서 동기화 | `npm run test:e2e:smoke` 결과 1 passed, 문서 반영 완료 |
