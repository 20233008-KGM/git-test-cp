-- =============================================================================
-- CampusConnect — 팀 채팅 Realtime publication (선택)
-- =============================================================================
-- 채팅 모달을 연 채로 다른 사용자 메시지가 자동으로 보이게 하려면 실행
-- 이미 publication에 포함되어 있으면 무시해도 됨
-- =============================================================================

ALTER PUBLICATION supabase_realtime ADD TABLE public.ai_team_detail_chat_messages;
